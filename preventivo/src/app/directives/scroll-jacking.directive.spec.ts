import { ScrollJackingDirective } from './scroll-jacking.directive';

describe('ScrollJackingDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollJackingDirective();
    expect(directive).toBeTruthy();
  });
});
